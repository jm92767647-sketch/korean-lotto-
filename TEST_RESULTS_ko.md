# 검증 결과

검증일: 2026-09-08. 테스트 입력: **11 13 19 20 31 44** (기존 요청의 예시, 실제 최신 회차 입력 아님).

## 계산 검증

| 항목 | 결과 |
|---|---|
| JavaScript 단위·회귀 테스트 | 10개 통과 / 실패 0개 |
| 작은 번호 공간 | 1~16 전체 조합; 필터 통과 1,568개 |
| 모든 작은 후보의 N4/N5 | 독립적인 집합 교집합 기준과 일치 |
| 모든 작은 후보의 marginal | 중복 커버를 포함한 독립 합집합 기준과 일치 |
| greedy | 독립적인 전수 합집합 선택과 같은 결과 |
| local search | 개선 및 99.5% 하한 확인; 종료 후 가능한 모든 1장 교체에 개선 없음 |
| Python 원본 작은 공간 비교 | U, greedy 3게임, 최종 3게임, C4/C5, 히스토그램 모두 일치 |
| 고정 seed 추가 초기해 | 같은 옵션으로 2회 실행한 티켓과 평가값 일치 |
| 32/33번 비트 경계 및 45번 | 정확한 popcount / 교집합 확인 |

필터 테스트는 3연속, 독립된 2연속 두 쌍, 전체 span 12/13, 최대 간격 19/20, 홀수 5개 등 극단 비율, 전주 번호 2개/3개 중복 경계를 포함합니다. 입력 테스트는 개수·범위·중복·소수·잘못된 형식을 포함합니다.

작은 공간의 최종 3게임은 Python과 JavaScript 모두 C4=914, C5=135, 최대 일치 개수별 히스토그램 `[0, 0, 16, 638, 779, 132, 3]`입니다.

## 전체 45개 번호 공간 및 Python 교차 검증

| 항목 | JavaScript 기본값: 기준 1회 + 추가 2회 |
|---|---:|
| 전체 조합 | 8,145,060 |
| 필터 통과 U | 5,239,128 |
| C4 | **100,842** |
| C5 | **2,350** |
| P4 | **1.924786%** |
| P5 | **0.044855%** |
| 최대 일치 정확히 4개 | 98,492 |
| 최대 일치 정확히 5개 | 2,340 |
| 6개 일치 | 10 |
| 탐색 중 최고 C4 | 100,842 |
| 고정 TypedArray 총 바이트 | 33,161,321 (약 33.16MB) |

JavaScript 최종 10게임을 **수정하지 않은 원본 Python**의 `generate_universe`와 `evaluate_portfolio`에 전달했습니다. 다음 값이 모두 일치했습니다.

- U 크기, C4, C5
- 전체 U의 최대 일치 개수별 히스토그램
- 각 티켓 N4/N5 및 출력 순서별 marginal C4
- 10×10 공통번호 행렬

원본 Python 기본 출력과 웹 기본 출력의 게임이 다를 수 있는 이유는 추가 초기해의 난수 생성기가 다르기 때문입니다. 평가 정의는 같습니다. 추가 재시작을 끈 기준 탐색은 Python의 기존 기준 결과와 같은 **C4=100,833, C5=2,350**으로, 브라우저에서도 확인했습니다.

원시 비교 자료:

- `tests/full-result.json`: JavaScript의 전체 탐색 결과
- `tests/python-reference.json`: 원본 Python 재평가 및 작은 공간 기준값
- `tests/browser-baseline.json`: 브라우저 실제 Worker 실행 결과

## 브라우저 기능

**Windows Microsoft Edge / Chromium 실제 브라우저**에서 확인했습니다.

- 빈 입력과 중복 번호 오류 표시
- Web Worker의 실제 전체 45개 번호 기준 탐색 및 정확 검산
- 계산 중 UI 조작, 즉시 중단, 재계산
- 10게임 클립보드 복사
- 평가값을 포함한 결과 파일 다운로드
- localStorage 저장 및 새로고침 후 결과 복원
- 320 / 360 / 390 / 430 / 768px 화면 폭에서 페이지 가로 넘침 없음
- 서비스 워커 캐시 완료 후 네트워크를 차단하고 새로고침 성공
- 네트워크 차단 상태에서 Worker 계산 시작 및 중단
- 외부 도메인 요청 0건, 페이지 오류 0건
- `file://` 직접 실행 + 네트워크 차단 상태에서 전체 Universe 생성 성공

**Windows Playwright WebKit 26.5 / 모바일 화면·터치 에뮬레이션**에서 확인했습니다.

- 실제 Worker에서 전체 45개 번호 기준 탐색 완료: U=5,239,128, C4=100,833, C5=2,350
- 모바일 화면 가로 넘침 없음
- 서비스 워커 제어 및 모든 앱 자산의 캐시 존재 확인
- 파일 전달 서버를 실제로 종료한 뒤 캐시에서 새로고침 성공
- 파일 전달 서버가 꺼진 상태에서 전체 Universe 생성 및 중단 성공

Playwright의 WebKit에서 `context.setOffline(true)`로 네트워크를 강제 차단한 뒤 탐색할 때는 **WebKit 내부 탐색 오류**가 발생했습니다. 이 경로를 통과했다고 표시하지 않습니다. 대신 파일 전달 서버를 실제로 종료한 상태의 캐시 재실행을 별도로 검증했습니다. Edge의 강제 오프라인 테스트는 통과했습니다.

Google Chrome에서도 네트워크를 차단한 **`file://` 직접 실행으로 기준 10게임 전체 계산**을 완료했습니다. C4=100,833, C5=2,350으로 일치했습니다. 이 실행에서는 localStorage 접근을 의도적으로 거부하고 자동 클립보드 기능도 비활성화했으며, 저장 제한 안내 및 번호를 직접 선택하는 복사 대체 UI가 정상 동작했습니다. 페이지 오류는 없었습니다. 브라우저별 상세 기록은 `tests/*checks.json`에 있습니다.

## 성능과 검증 범위

관측 실행 시간: Node.js의 추가 2회 전체 탐색 약 96.9초, Edge 기준 1회 약 45.7초, WebKit 기준 1회 약 27.7초. 서로 다른 프로세스·부하에서 실행한 기록이며 브라우저 성능 순위나 iPhone 예상 시간을 의미하지 않습니다. 메모리 숫자는 코드의 고정 배열 크기 합계이며, 브라우저 전체 메모리를 측정한 값이 아닙니다.

**실제 iPhone 기기의 Safari 및 홈 화면 설치를 직접 테스트하지는 못했습니다.** WebKit 엔진·모바일 화면 검증은 실기기 검증의 대체 증명이 아닙니다. 실제 iPhone의 화면 잠금, 장시간 배터리·메모리 압박, OS의 캐시 삭제 상황까지 보장하지 않습니다.

전역 최적성은 검증 대상이 아닙니다. 앱은 이를 보장하지 않으며, 99.5% 제약은 탐색에서 발견한 최고 C4 기준입니다. 최종 커버리지 정수값의 정확성과 최적해 탐색의 보장 여부를 분리했습니다.

## 재실행

```console
node --test tests/engine.test.cjs
node tests/full-run.cjs
```

원본 Python 프로그램을 별도로 보관하고 원본 의존성을 설치했다면:

```console
python tests/compare_python.py 원본_Python_파일이_있는_폴더
```

웹앱 사용자에게 Node.js·Python 설치를 요구하는 것은 아닙니다. 위 명령은 개발 검증을 다시 수행할 때만 필요합니다.
